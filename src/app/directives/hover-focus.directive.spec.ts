import { HoverFocusDirective } from './hover-focus.directive';

describe('Directive: HoverFocusDirective', async() => {

  it('should create an instance', () => {
    const directive = new HoverFocusDirective();
    expect(directive).toBeTruthy();
  });
});
